<!DOCTYPE html>
<html>
<head>
<title>Ecommerce</title>
<%= csrf_meta_tags %>
<%= csp_meta_tag %>


<%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
<%= javascript_importmap_tags %>
</head>


<body class="bg-gray-100 text-gray-800">
<nav class="bg-white shadow mb-8">
<div class="max-w-7xl mx-auto px-4 py-4 flex justify-between">
<h1 class="text-xl font-bold">🛒 MyStore</h1>
<div class="space-x-4">
<%= link_to "Products", products_path, class: "text-blue-600" %>
<% if user_signed_in? %>
<%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-red-500" %>
<% else %>
<%= link_to "Login", new_user_session_path %>
<%= link_to "Sign Up", new_user_registration_path %>
<% end %>
<%= link_to "Admin", admin_root_path, class: "font-semibold" %>
</div>
</div>
</nav>

<%= link_to "Cart", cart_path %>


<main class="max-w-7xl mx-auto px-4">
<%= yield %>
</main>
</body>
</html>